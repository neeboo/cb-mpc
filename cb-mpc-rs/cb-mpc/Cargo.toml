[package]
name = "cb-mpc"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Safe Rust API for Coinbase cb-mpc"

[dependencies]
cb-mpc-sys = { path = "../cb-mpc-sys" }
zeroize = "1.8"

[dev-dependencies]
k256 = { version = "0.13", features = ["ecdsa"] }
